{
  "name": "phraaaaaag",
  "license": "MIT",
  "engines": {
    "node": "10.15.3",
    "yarn": "1.15.2"
  },
  "scripts": {
    "build": "yarn --cwd fe install && yarn --cwd fe build",
    "deploy": "cp -a fe/build/. app/public/",
    "heroku-postbuild": "yarn build && yarn deploy"
  }
}
