{
  "name": "phraaaaaag",
  "license": "MIT",
  "engines": {
    "node": "10.15.3",
    "yarn": "1.15.2"
  },
  "scripts": {
    "heroku-postbuild": "cd fe && npm install && npm build && cd .. && mkdir app/main/public && cp -a fe/build/. app/main/public/ "
  }
}
