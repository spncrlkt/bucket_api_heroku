{
  "name": "phraaaaaag",
  "license": "MIT",
  "engines": {
    "node": "10.15.3",
    "yarn": "1.15.2"
  },
  "scripts": {
    "heroku-postbuild": "yarn --cwd fe install && yarn --cwd fe build && mkdir app/main/public && cp -a fe/build/. app/main/public/"
  }
}
